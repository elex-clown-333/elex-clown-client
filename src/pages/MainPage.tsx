import React from 'react';
import NavBar from "../components/NavBar";
const MainPage = () => {
    return (
        <React.Fragment>
            <NavBar/>
        </React.Fragment>
    );
};

export default MainPage;
